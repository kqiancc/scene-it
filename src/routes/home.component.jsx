import SearchBar from "../components/search-bar.component";

const Home = () => {
  return (
    <div className = "flex flex-col items-center">
       <SearchBar/>
    </div>
  );
};

export default Home;
